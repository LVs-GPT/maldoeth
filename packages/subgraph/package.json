{
  "name": "@maldo/subgraph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph codegen && graph build",
    "deploy": "graph deploy maldo-identity --studio",
    "create-local": "graph create maldo-identity --node http://localhost:8020",
    "deploy-local": "graph deploy maldo-identity --node http://localhost:8020 --ipfs http://localhost:5001"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.93.2",
    "@graphprotocol/graph-ts": "^0.35.1"
  }
}
